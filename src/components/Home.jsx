import { Box } from "@mui/material";
import TopBar from "./common/TopBar";
import Hero from "./homeComponents/Hero";

const Home = () => {
  return (
    <Box>
      <TopBar />
      <Hero />
    </Box>
  );
};

export default Home;
